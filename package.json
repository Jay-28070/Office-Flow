{
    "name": "officeflow",
    "version": "1.0.0",
    "scripts": {
        "start": "cd backend && node app.js",
        "build": "cd backend && npm install",
        "postinstall": "cd backend && npm install"
    },
    "engines": {
        "node": ">=14.0.0"
    }
}